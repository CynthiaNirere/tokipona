# reverse_string.txt
# Toki Pona Stack-Based - Reverse a String
#
# Stack trace for word = "hi" (length 2, last index 1):
#   idx=1: push reversed="", push word[1]='i', en -> "i", store reversed
#   idx=0: push reversed="i", push word[0]='h', en -> "ih", store reversed
#   Result: "ih"  ✓

kute word "Enter a word to reverse: "
pana ""
nimi reversed

# last index = len(word) - 1
kama word
len
pana 1
weka
nimi idx

# push starting condition: idx > -1
kama idx
pana -1
suli

sin
    # push reversed first, then char, then concat
    kama reversed
    kama word
    kama idx
    index
    en
    nimi reversed

    # decrement idx
    kama idx
    pana 1
    weka
    nimi idx

    # push new loop condition
    kama idx
    pana -1
    suli
pini

kama reversed
toki